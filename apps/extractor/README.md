
# Для запуска проекта выполнить

```bash
cd apps/extractor
python -m venv venv
venv\Scripts\activate
pip install requirements.txt
pip install -e .
```

# Тест
переходишь в ``` apps/extractor/main.py ```

в самом низу ```if __name__ == "__name__"```

там указаны документы, если надо меняешь и просто запускаешь
